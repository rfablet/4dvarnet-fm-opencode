# @package _global_
note: "Default config -- establish baseline"
training:
  stage1:
    epochs: 200
    lr: 0.001
  stage2:
    epochs: 400
    lr: 0.001
  batch_size: 32
model:
  hidden_channels: [64, 128, 256]
